(ns clj-money.models.reconciliations)

(defn create
  "Creates a new reconciliation record"
  [db-spec reconciliation]
  )

(defn find-by-account-id
  "Returns the reconciliations for the specified account"
  [dp-spec account-id]
  )
